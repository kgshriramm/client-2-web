import type { Metadata } from 'next';
import { notFound } from 'next/navigation';
import { poojas } from '../data';
export function generateStaticParams() { return poojas.map(([slug]) => ({ slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const p = poojas.find(([id]) => id === slug); return p ? { title: `${p[1]} in Gokarna | Gokarna Purohita`, description: `Enquire about ${p[1]} with a Gokarna Purohita.` } : {}; }
export default async function PoojaDetail({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const p = poojas.find(([id]) => id === slug); if (!p) notFound(); const [,name,image] = p; return <main className="pooja-detail"><header className="page-header"><a href="/">ॐ GOKARNA PUROHITHA</a><a href="/poojas">← Back</a></header><div className="detail-hero" style={{backgroundImage:`linear-gradient(#1d151190,#1d151190),url("${image}")`}}><p>GOKARNA POOJA SERVICE</p><h1>{name}</h1></div><article><h2>Enquire with the Purohita</h2><p>Each Pooja is arranged according to the devotee’s requirements and traditional procedure. Please contact the Purohita to confirm suitability, preparation, materials, duration and availability.</p><a href={`https://wa.me/919743029249?text=${encodeURIComponent(`Namaskara, I would like to enquire about ${name}.`)}`}>Connect on WhatsApp →</a></article><div className="page-actions"><a href="tel:+919743029249">☎ Call Now</a><a href={`https://wa.me/919743029249?text=${encodeURIComponent(`Namaskara, I would like to enquire about ${name}.`)}`}>WhatsApp</a></div></main>; }
